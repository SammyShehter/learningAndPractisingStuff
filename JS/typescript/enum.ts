enum Membership {
    lol,
    kek,
    cheburek
}

const member = Membership[2];

console.log(member);

enum Social {
    first = 'VK',
    second = 'Insta',
    third = 'Face'
}

const media = Social.first
console.log(media)